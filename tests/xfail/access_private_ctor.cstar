// An explicit constructor defaults to private — external `new` is then forbidden.
type value Singleton { int32 v; Singleton(){ this.v = 0; } }   // ctor private (no `public`)
fn int main() { Singleton s = Singleton(); return 0; }
