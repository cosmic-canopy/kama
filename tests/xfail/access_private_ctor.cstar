// An explicit constructor defaults to private — external `new` is then forbidden.
class Singleton { int32 v; Singleton(){ this.v = 0; } }   // ctor private (no `public`)
fn int main() { Singleton s = new Singleton(); return 0; }
