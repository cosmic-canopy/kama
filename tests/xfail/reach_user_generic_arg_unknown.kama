// KR-46: a user generic's type argument, naming a name declared nowhere. Must be rejected, naming it.
type value Box<X> { public X x; }
fn int32 f(Box<Zork> b) { return 0; }
fn int32 main() { return 0; }
