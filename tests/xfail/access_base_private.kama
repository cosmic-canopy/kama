// A subclass cannot reach a base class's PRIVATE field (only protected/public).
type virtual(maxDepth: 1) resource Base { int32 secret; public ctor make(int32 s){ this.secret = s; } }
type final resource Derived extends Base {
    public ctor make(int32 s){ this.base = Base.make(s: s); this.secret = s; }   // ERROR: secret is private in Base
    public fn int32 leak() { return this.secret; }
}
fn int32 main() { return 0; }
