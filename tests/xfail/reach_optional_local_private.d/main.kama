// KR-46: a type argument of a local (`Optional<X>`), naming a name private to `geo/lib.kama`, spelled bare. Must be rejected, naming it.
fn int32 main() { Optional<HidErr> x = Optional::None; return 0; }
