// A smart pointer (like any safe value) is never null — `== null` is a compile error.
type value Box { int32 v; public Box(int32 v){ this.v = v; } }
fn int main() { Owned<Box> p = new Box(v: 1); if (p == null) { return 1; } return 0; }
