// `hidden` is not on this file's surface. `mod_export_private.d` proves the IMPORT form is refused;
// this proves the qualified CALL is, which it was not — §1a claim 3 of design/module-system.md.
export { shown };

fn int32 hidden() { return 1; }
fn int32 shown()  { return 2; }
