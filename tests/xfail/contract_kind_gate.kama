type contract Drawable for resource { fn int32 draw(); }
type value Bad implements Drawable {
    int32 z;
    public ctor make(int32 z) { slot Bad r; r.z = z; return give r; }
    public fn int32 draw() { return this.z; }
}
fn int32 main() { return 0; }
