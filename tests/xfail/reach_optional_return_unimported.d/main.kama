// KR-46: a return type's type argument, never called, naming a name `geo` exports that this file does not import. Must be rejected with the import that fixes it.
fn Optional<ShownErr> f() { return Optional::None; }
fn int32 main() { return 0; }
