// KR-46: a container's element type (`DynamicArray<X>`), naming a name private to `geo/lib.kama`, spelled qualified. Must be rejected, naming it.
import { std::collections::DynamicArray };
fn int32 main() { DynamicArray<geo::HidErr> a = DynamicArray.empty(); return 0; }
