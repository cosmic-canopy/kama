// A private field is not readable from outside the class (M25 — private by default).
type value Account { int32 balance; public ctor make(int32 b){ Account r; r.balance = b; return give r; } }
fn int main() { Account a = Account.make(b: 100); return a.balance; }   // balance is private
