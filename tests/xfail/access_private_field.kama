// A private field is not readable from outside the class (M25 — private by default).
type value Account { int32 balance; public Account(int32 b){ this.balance = b; } }
fn int main() { Account a = Account(b: 100); return a.balance; }   // balance is private
