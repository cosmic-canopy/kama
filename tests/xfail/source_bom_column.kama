fn int32 main() { return 1 $ 2; }
// A UTF-8 byte-order mark is skipped BEFORE the lexer, so a line-1 error keeps the column it has without one
// (25). tools/check-source-encoding.sh holds the mark down.
