// A plain (sealed) class cannot be extended; only virtual/abstract classes may.
type value Base { public ctor make() { } }
type virtual(maxDepth: 1) resource Derived extends Base { protected virtual fn int32 h() { return 0; } }
fn int32 main() { return 0; }
