// A primitive owns nothing, so there is nothing to destroy.
type contract C for value { fn int32 f(); }
type intrinsic <int32> implements C {
    public fn int32 f() { return 1; }
    ~C() { }
}
fn int32 main() { return 0; }
