// KR-46: an `Owned<X>` field of a resource, naming a name private to `geo/lib.kama`, spelled bare. Must be rejected, naming it.
type resource W { Owned<HidVal> p; public ctor make(Owned<HidVal> p) { this.p = give p; } }
fn int32 main() { return 0; }
