// The accessor must resolve to a real class / function / method.
class Account { int64 balance; public Account(int64 b){ this.balance = b; } friend Nonexistent[balance]; }
fn int main() { Account acc = new Account(b: 1); return 0; }
