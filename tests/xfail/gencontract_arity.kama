// A generic contract must be given exactly as many type arguments as it has parameters.
// `Seq<int32, int32>` supplies two args to a one-parameter contract — a compile-time error.
type contract Seq<T> for value { fn Optional<T> next(); }

fn void take(Seq<int32, int32> it) { }

fn int32 main() { return 0; }
