// KR-46: a local inside a generic function, called, naming a name declared nowhere. Must be rejected, naming it.
fn int32 g<X>(X x) { Optional<Zork> o = Optional::None; return 0; }
fn int32 main() { return g(x: 1); }
