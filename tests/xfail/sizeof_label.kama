// The pointer form of `sizeof` is spelled `sizeof(ptr: p)`, like `drop(ptr: p)` — any other label is refused.
unsafe fn usize measure(UnsafePtr<int32> p) { return sizeof(of: p); }
fn int32 main() { return 0; }
