import std::memory::{Owned};
// `give`/`copy` mark a NAMED value — a fresh constructor/call result needs no marker.
type value C { int32 n; public ctor make(int32 n){ this.n = n; } }
fn int32 main() { Owned<C> a = give new C.make(n: 1); return 0; }
