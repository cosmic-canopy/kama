import std::memory::{Owned};
// `give`/`copy` mark a NAMED value — a fresh constructor/call result needs no marker.
type value C { int32 n; public ctor make(int32 n){ C r; r.n = n; return give r; } }
fn int main() { Owned<C> a = give new C.make(n: 1); return 0; }
