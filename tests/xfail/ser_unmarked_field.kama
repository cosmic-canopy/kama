// A field with no @field/@skip on a @generate type is a compile error (explicit-intent rule).
@generate(Serializable)
type value Bad {
    @field int32 x;
    int32 unmarked;
}
fn int32 main() { return 0; }
