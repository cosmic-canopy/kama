// A plain (sealed) class cannot be extended; only virtual/abstract classes may.
type value Base { public Base() {} }
type virtual resource Derived extends Base { protected virtual fn int h() { return 0; } }
fn int main() { return 0; }
