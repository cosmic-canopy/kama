// KR-46: a field type, naming a name private to `geo/lib.kama`, spelled bare. Must be rejected, naming it.
type value W { public HidErr e; public ctor make() { this.e = HidErr::Bad; } }
fn int32 main() { W w = W.make(); return 0; }
