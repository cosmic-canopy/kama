// KR-46: a return type's type argument, called, naming a name private to `geo/lib.kama`, spelled bare. Must be rejected, naming it.
fn Optional<HidErr> f() { return Optional::None; }
fn int32 main() { return match (f()) { case Some(value: v): 1; case None: 0; }; }
