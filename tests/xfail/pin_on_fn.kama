// Same rule as xfail/pin_on_value, at the other place a type-parameter list is accepted: a free function
// has no implementing type for `This` to name.
fn int32 weigh<T is This>(T x) { return 0; }

fn int32 main() { return 0; }
