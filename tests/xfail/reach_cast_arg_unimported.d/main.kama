// KR-46: a `cast<UnsafePtr<X>>` type argument, naming a name `geo` exports that this file does not import. Must be rejected with the import that fixes it.
unsafe fn int32 f(UnsafePtr p) { UnsafePtr w = cast<UnsafePtr>(cast<UnsafePtr<ShownVal>>(p)); return 0; }
fn int32 main() { return 0; }
