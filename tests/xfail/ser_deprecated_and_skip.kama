// `@skip` is absent from every backend; `@deprecated` is still READ. A field cannot be both, and letting
// `@skip` win silently would drop a field the author explicitly asked to keep reading.
@generate(Serializable, Deserializable)
type value R { @field @deprecated @skip int32 a; }
fn int32 main() { return 0; }
