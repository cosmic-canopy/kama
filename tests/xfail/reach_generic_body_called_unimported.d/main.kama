// KR-46: a local inside a generic function, called, naming a name `geo` exports that this file does not import. Must be rejected with the import that fixes it.
fn int32 g<X>(X x) { Optional<ShownErr> o = Optional::None; return 0; }
fn int32 main() { return g(x: 1); }
