// An overridable method must be `protected`, never public (public polymorphism = interface).
type virtual(maxDepth: 1) resource Shape { public virtual fn int area() { return 1; } }
fn int main() { return 0; }
