// An overridable method must be `protected`, never public (public polymorphism = interface).
type virtual(maxDepth: 1) resource Shape { public virtual fn int32 area() { return 1; } }
fn int32 main() { return 0; }
