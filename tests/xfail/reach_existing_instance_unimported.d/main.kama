// KR-46: a type argument of a `Result` instance that already exists, naming a name `geo` exports that this file does not import. Must be rejected with the import that fixes it.
import { geo::Shown };
fn int32 main() { Result<Shown, ShownErr> r = Shown.parse(n: 1); return 0; }
