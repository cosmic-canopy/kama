// M26h — `~dtor` ⟺ `resource`. A value owns nothing, so a destructor makes it a resource.
type value Bad { int32 n; public ctor make(int32 n){ this.n = n; } ~Bad() { } }
fn int32 main() { Bad b = Bad.make(n: 1); return 0; }
