// Same rule, the CONSTRUCTION position: dot-on-type. It resolves its receiver with no site node, so it
// does not arrive through the resolve wrappers' gate and needs the predicate called directly.
export { shown };

type value Hidden { public int32 n; public ctor make(int32 n) { this.n = n; } }
fn int32 shown() { return 2; }
