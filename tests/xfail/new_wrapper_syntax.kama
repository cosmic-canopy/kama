import std::memory::{Owned};
// `new` now names the ELEMENT type, not the smart-pointer wrapper (M26a).
type value Box { int32 v; public Box(int32 v) { this.v = v; } }
fn int main() { Owned<Box> p = new Owned<Box>(v: 1); return 0; }   // ERROR: write `new Box(v: 1)`
