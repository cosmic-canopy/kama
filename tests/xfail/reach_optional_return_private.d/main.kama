// KR-46: a return type's type argument, never called, naming a name private to `geo/lib.kama`, spelled bare. Must be rejected, naming it.
fn Optional<HidErr> f() { return Optional::None; }
fn int32 main() { return 0; }
