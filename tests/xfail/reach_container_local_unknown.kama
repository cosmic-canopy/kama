// KR-46: a container's element type (`DynamicArray<X>`), naming a name declared nowhere. Must be rejected, naming it.
import { std::collections::DynamicArray };
fn int32 main() { DynamicArray<Zork> a = DynamicArray.empty(); return 0; }
