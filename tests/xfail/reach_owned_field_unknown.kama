// KR-46: an `Owned<X>` field of a resource, naming a name declared nowhere. Must be rejected, naming it.
type resource W { Owned<ZorkVal> p; public ctor make(Owned<ZorkVal> p) { this.p = give p; } }
fn int32 main() { return 0; }
