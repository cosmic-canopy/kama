// KR-46: a local inside a generic function, never called, naming a name private to `geo/lib.kama`, spelled bare. Must be rejected, naming it.
fn int32 g<X>(X x) { Optional<HidErr> o = Optional::None; return 0; }
fn int32 main() { return 0; }
