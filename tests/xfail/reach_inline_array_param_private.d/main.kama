// KR-46: an `InlineArray<X>#(2)` parameter's element type, naming a name private to `geo/lib.kama`, spelled bare. Must be rejected, naming it.
fn int32 f(InlineArray<HidErr>#(2) a) { return 0; }
fn int32 main() { return 0; }
