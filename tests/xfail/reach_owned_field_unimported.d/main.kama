// KR-46: an `Owned<X>` field of a resource, naming a name `geo` exports that this file does not import. Must be rejected with the import that fixes it.
type resource W { Owned<ShownVal> p; public ctor make(Owned<ShownVal> p) { this.p = give p; } }
fn int32 main() { return 0; }
