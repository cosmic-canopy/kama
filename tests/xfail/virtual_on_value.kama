// M26h — `virtual`/`abstract`/`final` are qualifiers on a `resource`; a value is sealed.
type virtual value Bad { int32 n; public ctor make(int32 n){ Bad r; r.n = n; return give r; } }
fn int32 main() { return 0; }
