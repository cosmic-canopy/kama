// Declaring the contract obliges the block to supply every method it requires.
type contract C for value, intrinsic { fn int32 f(); fn int32 g(); }
type intrinsic <int32> implements C {
    public fn int32 f() { return 1; }
}
fn int32 main() { return 0; }
