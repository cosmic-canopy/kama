// KR-46: a generic function's bound, never called, naming a name `geo` exports that this file does not import. Must be rejected with the import that fixes it.
fn int32 g<X: ShownC>(X x) { return x.cm(); }
fn int32 main() { return 0; }
