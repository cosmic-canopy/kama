// KR-46: a type argument of a `Result` instance that already exists, naming a name private to `geo/lib.kama`, spelled bare. Must be rejected, naming it.
import { geo::Shown };
fn int32 main() { Result<Shown, HidErr> r = Shown.parse(n: 1); return 0; }
