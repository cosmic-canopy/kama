// An enum with payloads is a tag plus a union kama lays out — no C header can state it — so it cannot cross by
// value. (A payload-less enum crosses only when someone owns its values: `type extern enum` for a C header's,
// `type expose enum` for kama's — tests/xfail/crossing_plain_enum.kama.)
extern "stdio.h";
type enum Shape { Circle(int32 r), Point }
extern fn int32 takes_shape(Shape s);
fn int32 main() { return 0; }
