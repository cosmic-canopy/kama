// Session A — a local declaration shadowing a parameter is a compile error.
fn int32 f(int32 n) {
    int32 n = 5;   // ERROR: shadows the parameter `n`
    return n;
}
fn int32 main() { return f(n: 1); }
