// M26h — a `value` owns nothing; owning a collection (a resource) is a compile error.
import std::collections::{DynamicArray};
type value Bad { DynamicArray<int32> items; public ctor make() { Bad r; r.items.add(item: 1); return give r; } }
fn int32 main() { Bad b = Bad.make(); return 0; }
