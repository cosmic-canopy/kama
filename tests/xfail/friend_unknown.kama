// The accessor must resolve to a real class / function / method.
type value Account { int64 balance; public Account(int64 b){ this.balance = b; } friend Nonexistent[balance]; }
fn int main() { Account acc = Account(b: 1); return 0; }
