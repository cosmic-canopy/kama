// M26h — `~dtor` ⟺ `resource`. A value owns nothing, so a destructor makes it a resource.
type value Bad { int32 n; public Bad(int32 n){ this.n = n; } ~Bad() { } }
fn int32 main() { Bad b = Bad(n: 1); return 0; }
