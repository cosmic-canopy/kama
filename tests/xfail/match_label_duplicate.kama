// Binding the same field twice is a mistake, not a shorthand for two views of one value.
type enum Shape { Rect(int32 w, int32 h) }
fn int32 main() {
    Shape s = Shape::Rect(w: 2, h: 3);
    return match (s) { case Rect(w: a, w: b): a; };   // ERROR: `w` bound twice
}
