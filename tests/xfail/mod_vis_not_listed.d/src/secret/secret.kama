// `shown` IS exported, so the file rung is satisfied. What refuses this import is the MODULE's
// `visibility`, which names `other` and not the root — the manifest key that was parsed, validated for
// self-consistency, and consulted by nothing until phase 3c.
export { shown };

fn int32 shown() { return 2; }
