// KR-46: a type argument of a `Result` instance that already exists, naming a name declared nowhere. Must be rejected, naming it.
import { geo::Shown };
fn int32 main() { Result<Shown, Zork> r = Shown.parse(n: 1); return 0; }
