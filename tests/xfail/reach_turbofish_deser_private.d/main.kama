// KR-46: a serde turbofish type argument, naming a name private to `geo/lib.kama`, spelled bare. Must be rejected, naming it.
import { std::serialization::text::json::deserializeJsonBuffer };
fn int32 main() { string s = "{\"v\":1}"; deserializeJsonBuffer::<HidVal>(src: give s); return 0; }
