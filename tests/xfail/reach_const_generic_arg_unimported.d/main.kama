// KR-46: a const-generic `#(K)` argument, naming a name `geo` exports that this file does not import. Must be rejected with the import that fixes it.
fn int32 f(InlineArray<int32>#(SHOWNK) a) { return 0; }
fn int32 main() { return 0; }
