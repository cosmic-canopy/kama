// FFI: pass a cstar string to a C const char* via .cstr() (header-based).
extern "<string.h>";
extern usize strlen(Ptr<int8> s);
int main() { string s = "**"; int32 n = cast<int32>(strlen(s: s.cstr())); return n * 21; }  // 2*21=42
