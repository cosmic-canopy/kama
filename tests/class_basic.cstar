class Counter
{
    int value;
    public Counter(int start)
    {
        value = start;
    }
    public fn void add(int n)
    {
        value = value + n;
    }
    public fn int get()
    {
        return value;
    }
}
fn int main()
{
    Counter c = new Counter(start: 40);
    c.add(n: 2);
    return c.get();
}
