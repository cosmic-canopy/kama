class Counter
{
    int value;
    Counter(int start)
    {
        value = start;
    }
    fn void add(int n)
    {
        value = value + n;
    }
    fn int get()
    {
        return value;
    }
}
fn int main()
{
    Counter c = new Counter(start: 40);
    c.add(n: 2);
    return c.get();
}
