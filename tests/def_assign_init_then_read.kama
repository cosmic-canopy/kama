import std::memory::{Owned};
// A local constructed at its declaration is definitely assigned -> reads are fine.
type value Box { int32 v; public Box(int32 v) { this.v = v; } public fn int32 get() { return this.v; } }
fn int32 main() {
    Owned<Box> b = new Box(v: 41);
    return b.get() + 1;   // 42
}
