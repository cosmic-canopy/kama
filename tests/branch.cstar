fn int classify(int n)
{
    if (n > 0) { return 1; }
    else { return 0; }
}
fn int main()
{
    int i = 0;
    int count = 0;
    while (i < 10)
    {
        if (classify(n: i) == 1) { count += 1; }
        i++;
    }
    return count;
}
