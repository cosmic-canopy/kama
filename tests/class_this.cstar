class Accum
{
    int total;
    Accum(int init)
    {
        total = init;
    }
    fn void bump()
    {
        this.total = this.total + 1;
    }
    fn int run()
    {
        this.bump();
        return this.total;
    }
}
fn int main()
{
    Accum a = new Accum(init: 40);
    a.bump();
    return a.run();
}
