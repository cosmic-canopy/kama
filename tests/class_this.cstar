class Accum
{
    int total;
    Accum(int init)
    {
        total = init;
    }
    void bump()
    {
        this.total = this.total + 1;
    }
    int run()
    {
        this.bump();
        return this.total;
    }
}
int main()
{
    Accum a = new Accum(init: 40);
    a.bump();
    return a.run();
}
