type value Point { public int32 x; public int32 y; public Point(int32 x, int32 y){this.x=x;this.y=y;} public fn int32 sum(){return this.x+this.y;} }
fn Point mk(int32 a) { return Point(x: a, y: a + 1); }
fn int main() { Point p = mk(a: 20); return p.sum(); }
