@generate(of) type value Point { public int32 x; public int32 y; public fn int32 sum(){return this.x+this.y;} }
fn Point mk(int32 a) { return Point.of(x: a, y: a + 1); }
fn int32 main() { Point p = mk(a: 20); return p.sum(); }
