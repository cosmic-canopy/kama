interface IShape
{
    fn int64 area();
}
class Circle implements IShape
{
    int64 r;
    public Circle(int64 r) { this.r = r; }
    fn int64 area() { return r * r; }
}
class Square implements IShape
{
    int64 s;
    public Square(int64 s) { this.s = s; }
    fn int64 area() { return s * s; }
}
fn int64 measure(IShape sh)
{
    return sh.area();
}
fn int main()
{
    Circle c = new Circle(r: 7);
    Square q = new Square(s: 5);
    int64 a = measure(sh: c);
    int64 b = measure(sh: q);
    uint64 sum = cast<uint64>(a) + cast<uint64>(b);
    uint64 m = sum % 256;
    return cast<int>(m);
}
