extern fn void kama_trace(int code);
extern fn int kama_trace_get();

type resource Res
{
    int id;
    public Res(int id) { this.id = id; }
    ~Res() { kama_trace(code: id); }
}

fn int f()
{
    Res outer = Res(id: 1);
    while (true)
    {
        Res inner = Res(id: 2);
        break;
    }
    return 0;
}

fn int main()
{
    int r = f();
    return kama_trace_get();
}
