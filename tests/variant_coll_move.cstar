enum Bag { Items(List<int32> xs), Empty }
fn int main()
{
    List<int32> xs;
    xs.add(item: 40); xs.add(item: 2);
    Bag b = Bag::Items(xs: give xs);
    return match (b) { case Items(ys): ys[0] + ys[1]; case Empty: 0; };
}
