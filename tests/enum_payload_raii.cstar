type resource Probe
{
    int32 id;
    public Probe(int32 id) { this.id = id; }
    ~Probe() { }
}
enum Box
{
    Full(Shared<Probe> p),
    Empty
}
fn int main()
{
    Shared<Probe> s = new Probe(id: 7);
    Box a = Box::Full(p: give s);
    Box b = Box::Empty;
    return 0;
}
