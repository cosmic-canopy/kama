class Base
{
    int x;
    Base(int x) { this.x = x; }
}
class Derived extends Base
{
    int y;
    Derived(int x, int y) : base(x: x)
    {
        this.y = y;
    }
    fn int sum() { return x + y; }
}
fn int main()
{
    Derived d = new Derived(x: 30, y: 12);
    return d.sum();
}
