import { Holder, makeHolder, Res, makeRes, takesKind };
export { viaHolder };

fn int32 viaHolder() {
    Holder h = makeHolder();
    Res r = makeRes();
    return h.n + r.get() + takesKind(k: h.k);   // 20 + 22 + 1
}
