fn int pick(int x)
{
    if (x == 1) { return 10; }
    else if (x == 2) { return 20; }
    else { return 0; }
}
fn int main()
{
    int total = 0;
    int i = 0;
    do
    {
        total += pick(x: i);
        i++;
    } while (i < 4);
    return total;
}
