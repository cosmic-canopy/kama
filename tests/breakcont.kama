fn int32 main()
{
    int32 sum = 0;
    int32 i = 0;
    while (true)
    {
        i++;
        if (i > 20) { break; }
        if ((i % 2) == 0) { continue; }
        sum += i;
    }
    return sum;
}
