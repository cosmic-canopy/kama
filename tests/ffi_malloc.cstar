// FFI core: hold an opaque Ptr (void*) from malloc, size_t, null-check, free.
extern "<stdlib.h>";
extern Ptr malloc(usize n);
extern void free(Ptr p);

int main()
{
    Ptr p = malloc(n: 64);
    if (p == null) { return 1; }
    free(p: p);
    return 42;
}
