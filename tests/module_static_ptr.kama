// MCU step 1: a `Ptr` static (a peripheral handle) — zero-inits to null, then holds an assigned address.
static Ptr<int32> target;

fn int32 main() {
    int32 x = 42;
    target = addr(of: x);
    unsafe { return target[0]; }   // 42
}
