// Integer remainder by zero traps just like division. Runtime-opaque zero divisor so it isn't folded.
import std::num::{int32Min};
fn int main() {
    int32 zero = int32Min() - int32Min();   // 0, opaque
    int32 a = 10;
    return a % zero;                          // mod-by-zero traps
}
