// The json sibling of tests/noheap_flag_import_uuid.d: the text-json back end allocates on nearly every path
// (error boxes, a `substring`, a growing buffer), and was refused 28 times under `--no-heap` for being imported.
// Nothing here calls it, so nothing here is reached (KR-47).
import { std::serialization::text::json::deserializeJsonBuffer };
fn int32 main() { return 42; }
