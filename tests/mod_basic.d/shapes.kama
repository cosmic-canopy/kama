namespace Shapes;
export { Box };

type value Box
{
    int32 w;
    int32 h;
    public ctor make(int32 w, int32 h) { Box r; r.w = w; r.h = h; return give r; }
    public fn int32 area() { return this.w * this.h; }
}
