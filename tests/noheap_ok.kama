// An `@noheap` hot path (a game frame tick / audio callback): the compiler PROVES it never allocates.
@noheap fn int32 tick(int32 n) { int32 acc = 0; int32 i = 0; while (i < n) { acc = acc + i * 2; i = i + 1; } return acc; }
fn int32 main() { return tick(n: 5); }   // 0+2+4+6+8 = 20
