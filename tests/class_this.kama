type value Accum
{
    int total;
    public ctor make(int init)
    {
        this.total = init;
    }
    public fn void bump()
    {
        this.total = this.total + 1;
    }
    public fn int run()
    {
        this.bump();
        return this.total;
    }
}
fn int main()
{
    Accum a = Accum.make(init: 40);
    a.bump();
    return a.run();
}
